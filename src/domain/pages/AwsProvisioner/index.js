import React from 'react';
import { view } from 'redux-elm';

export const title = 'AWS Provisioner';
export const path = 'aws-provisioner';
export const icon = 'server';
export const description = `Manage worker-types known to the AWS Provisioner and check on the status of AWS nodes.`;

export default view(({ model, dispatch }) => <div></div>);
