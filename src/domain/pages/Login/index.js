import React from 'react';
import { view } from 'redux-elm';

export const title = 'Login';
export const path = 'login';
export const icon = 'users';
export const description = `Register an authenticated user.`;

export default view(() => <h2>Logging in...</h2>);
