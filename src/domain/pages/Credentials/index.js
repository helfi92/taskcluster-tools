import React from 'react';
import { view } from 'redux-elm';

export const title = 'Credentials';
export const path = 'credentials';
export const icon = 'key';
export const description = `Manage credentials stored in the browser.`;

export default view(({ model, dispatch }) => <div></div>);
