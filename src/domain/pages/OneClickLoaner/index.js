import React from 'react';
import { view } from 'redux-elm';

export const title = 'One-Click Loaner';
export const path = 'one-click-loaner';
export const icon = 'terminal';
export const description = `Create a loaner from an existing task.`;

export default view(({ model, dispatch }) => <div></div>);
