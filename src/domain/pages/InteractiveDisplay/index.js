import React from 'react';
import { view } from 'redux-elm';

export const title = 'Interactive Display';
export const path = 'display';
export const icon = 'television';
export const description = `Connect to an interactive display within tasks.`;

export default view(({ model, dispatch }) => <div></div>);
