import React from 'react';
import { view } from 'redux-elm';

export const title = 'Hooks Manager';
export const path = 'hooks';
export const icon = 'repeat';
export const description = `Manage hooks, i.e. tasks that are created in response to events within TaskCluster.`;

export default view(({ model, dispatch }) => <div></div>);
