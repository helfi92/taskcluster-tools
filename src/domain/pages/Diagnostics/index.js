import React from 'react';
import { view } from 'redux-elm';

export const title = 'Diagnostics';
export const path = 'diagnostics';
export const icon = 'cogs';
export const description = `Display results from diagnostics tests.`;

export default view(({ model, dispatch }) => <div></div>);
