import React from 'react';
import { view } from 'redux-elm';

export const title = 'Interactive Shell';
export const path = 'interactive';
export const icon = 'terminal';
export const description = `Open an interactive shell within tasks.`;

export default view(({ model, dispatch }) => <div></div>);
